{
  "name": "benefits-qa-eval",
  "version": "1.0.0",
  "description": "Evaluating whether an LLM answers health plan coverage questions correctly when given real Summary of Benefits and Coverage (SBC) documents as retrieved context (RAG), across a 6-plan document library.",
  "private": true,
  "scripts": {
    "build-index": "node scripts/build-index.js",
    "eval": "promptfoo eval",
    "view": "promptfoo view"
  },
  "devDependencies": {
    "promptfoo": "latest"
  }
}
